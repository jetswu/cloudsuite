server {
    listen 80;
    server_name _;

    location /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }

    location / {
        return 503 "CloudSuite — service belum siap.\n";
        add_header Content-Type text/plain;
    }
}
